name: Lock Threads

on: [pull_request, issues]

permissions:
  issues: write
  pull-requests: write

jobs:
  lock_threads:
    runs-on: ubuntu-latest
    steps:
      - name: Lock thread
        uses: dessant/lock-threads@v2
        with:
          # Issue specific options
          issue-lock-inactive-days: 30 # 30 days before locking
          issue-exclude-labels: "Do not lock this"
          issue-lock-comment: >
            This issue has been automatically locked since there
            has not been any recent activity after it was closed.
            Please open a new issue for related bugs
          issue-lock-labels: "Outdated and locked"
          # PR specific options
          pr-lock-inactive-days: 60 # wait 2 months aprox.
          pr-exclude-labels: "Do not lock this"
          pr-lock-comment: >
            This pull request has been automatically locked since there
            has not been any recent activity after it was closed.
            Please open a new issue for related bugs.
          pr-lock-labels: "Outdated and locked"
      - name: Post-workflow message
        run: echo "Workflow completed!"
